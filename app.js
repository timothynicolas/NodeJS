// CommonJS, every file is a module (by default)
// MOddules - Encapsulated code (only share minimum)

const john = 'john';

const sayHi = (name) => {
  console.log(`Hello there, ${name}`);
}

sayHi(john);